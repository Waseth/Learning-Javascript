// What does 'resultVariable' equal? What data type is it?
// We haven't covered everything here in detail yet, but research this for a bit and see what you find.

const variable1 = 20;
const variable2 = '40';

const resultVariable = variable1 + variable2;
console.log(resultVariable);

// What's printed out is:
// '2040'