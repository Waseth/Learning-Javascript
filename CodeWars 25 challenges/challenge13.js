function removeChar(str){
  let newStr = str.slice(1,-1);
  return newStr;
};

// I have learned how slice() works.The first number in the brackets is the starting index and it is inclusive while the last one is exclusive and that's where basically we stop slicing from.
