// Why does this code not work? Try to make it work and guess what the result will be.
// (hint: this is a little bit of a review from last lesson).

// const numberVariable = 0;

// numberVariable ++;
// numberVariable ++;
// numberVariable ++;

// console.log(numberVariable);

// It doesn't work because a variable that has been declared using the const keyword cannot be increamented

// Here is my solution

let numberVariable = 0;

numberVariable ++;
numberVariable ++;
numberVariable ++;

console.log(numberVariable); 